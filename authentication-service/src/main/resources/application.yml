server:
  port: 8083
spring:
  application:
    name: authentication-service
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${eureka.server}

